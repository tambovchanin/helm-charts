apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xui.fullname" . }}-test-connection"
  labels:
    {{- include "xui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "xui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
